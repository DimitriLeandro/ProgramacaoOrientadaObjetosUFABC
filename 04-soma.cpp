#include <iostream>

using namespace std;

int main(){

	long a, b, soma;
	
	cout << "Insira dois numeros separados por um espaco para fazer a soma: ";
	cin >> a >> b;
	
	soma = a + b;
	cout << soma << "\n";

	return 0;
}